

public class Circulo {
	
	private Punto punto;
	private float radio;
	
	public Circulo(Punto _punto, float _radio) {
		this.punto = _punto;
		this.radio = _radio;
	}

	public boolean intersectaCon(Circulo circulo) {
		float cordenadaX = this.punto.getCordenadaX();
		float cordenadaY = this.punto.getCordenadaY();
		
		System.out.println(puntoX);
		return true;
	}
	
	public static void main(String[] args) {
		Circulo circle = new Circulo(new Punto(2,2), 2);
		
		circle.intersectaCon(circle);
	}

}

